# Archived PostgreSQL migrations

These SQL files are retained only as rollback/reference history for the former
PostgreSQL database. The active Drizzle configuration does not execute this
directory. MySQL migrations are generated into `src/db/migrations-mysql`.
