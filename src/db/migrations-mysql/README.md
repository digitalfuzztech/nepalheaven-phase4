# Active MySQL migrations

This directory is the executable Drizzle migration history for MySQL 8. The
`0000` migration is a fresh baseline of the complete current application
schema and is intended for an empty MySQL database.

Do not run the archived PostgreSQL migrations in `../migrations` against
MySQL.
